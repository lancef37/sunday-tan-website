# Client-side Environment Variables for Local Development
# Copy this to .env.local for local development

# API URL - Points to local backend server
NEXT_PUBLIC_API_URL=http://localhost:5000

# App URL - Frontend URL for redirects and links
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Square Payment (for client-side Square Web SDK if needed)
# These are public keys, safe to expose in browser
NEXT_PUBLIC_SQUARE_APPLICATION_ID=sandbox-sq0idb-xxxxx
NEXT_PUBLIC_SQUARE_LOCATION_ID=L_xxxxx